# Yaad — Billing & Abuse Controls milestone
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29
httpx>=0.27
sqlalchemy>=2.0,<3.0
redis>=5.0
python-multipart>=0.0.9      # UploadFile / multipart form parsing
psycopg[binary]>=3.1         # prod Postgres driver (sqlite used in dev by default)
